/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://andrewpr0.art', // Cambia esto por el dominio de tu página
  generateRobotsTxt: true,         // Crea automáticamente el archivo robots.txt
  sitemapSize: 5000,               // Número máximo de URLs por archivo (opcional)
};